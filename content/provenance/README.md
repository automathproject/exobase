Provenance stable des contenus normalisés.

Ce répertoire est destiné aux manifestes qui relient les fichiers de `content/`
aux sources importées. Il ne remplace pas les métadonnées pédagogiques générées
dans `openyourmath-v2`.
